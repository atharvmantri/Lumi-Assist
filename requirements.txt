# Lumi — Python 3.11 dependencies
# Install on Windows with:
#   py -3.11 -m pip install -r requirements.txt
#
# This file is intentionally CPU-safe. NVIDIA/CUDA runtime wheels live in
# requirements-gpu.txt and are installed by install.bat only when an NVIDIA GPU
# is detected. That keeps fresh installs much smaller and avoids making CUDA a
# hard requirement for reviewers/users on ordinary Windows laptops.

# ===== Core LLM / config =====
openai>=1.0.0
python-dotenv>=1.0.0
PyYAML>=6.0.1
requests>=2.31.0

# ===== STT =====
faster-whisper>=1.0.0
numpy>=1.26.0,<2.0

# ===== Audio I/O =====
sounddevice>=0.4.6

# ===== TTS =====
piper-tts>=1.2.0

# ===== Wake word =====
openwakeword>=0.6.0
onnxruntime>=1.17.0

# ===== UI =====
PyQt6>=6.6.0

# ===== PC automation =====
pyautogui>=0.9.54
pywinauto>=0.6.8
keyboard>=0.13.5
pycaw>=20240910
comtypes>=1.2.0
pyperclip>=1.8.2
Pillow>=10.0.0
pytesseract>=0.3.10
win10toast>=0.9

# ===== Web =====
duckduckgo-search>=6.0.0
ddgs>=9.0.0

# ===== Optional GPU acceleration =====
# For NVIDIA GPU support, additionally run:
#   venv\Scripts\python.exe -m pip install -r requirements-gpu.txt
# faster-whisper itself does not require torch.
